Day 1:

Prepare file structure.

Should include notebooks, scripts, data, tutorial and readme

Install git on mac if not already installed

Establish ssh key for aithentication. Follow github instructions. Might have to generate a key for use.
Commands used:
> ls -al ~/.ssh
if no key found, genearte a new key
> ssh-keygen -t ed25519 -C "email id"
Add ssh key to ssh agent
> eval "$(ssh-agent -s)"
> ssh-add ~/.ssh/id_ed25519
Add ssh key to github
> cat ~/.ssh/id_ed25519.pub
Copy the output
Got to Github, add new ssh key (under settings/SSh key tabs)
save
Test connection
> ssh -T git@github.com

Create a new repository on github and name the samei as the local folder

git init
git add README.md
git commit -m “first commit”
git branch -M main
git remote add origin git@github.com:SomaG-PSN/Oxygen_sensitivity_infection_cancer.git
git push -u origin main

Add all folders in the repository

git add .
git commit -m "all files added"
git push -u origin main

Add updated tutorial

 
